# train/ — v2 placeholder

Reserved for the **v2 learned cinematography policy** (targeted week of 2026-06-26).

v2 consumes the v1 shot logs in `runs/<ts>/shots.jsonl` (frozen schema v1.0.0,
LeRobotDataset-aligned) as training data — no separate data-collection phase.
This is the ONLY part that needs the AMD GPU / ROCm; all v1 inference is CPU.

Nothing here yet. Out of scope for v1.
